# ANIMAL LEAGUE 본선 토너먼트 웹앱 — 개발 명세

2026 LIKELION UNIV. 14th Hackathon 본선(**8/25, 코엑스 마곡**)에서 사용할 심사·브래킷 운영 도구.
디자인은 기존 디자인 에셋(브랜드 컬러, 폰트, 캐릭터 일러스트)을 적용하므로, 이 문서는 **기능·데이터·로직**에 집중한다.

> 이 문서가 요구사항의 단일 출처다. 구현 판단이 갈릴 때는 여기를 먼저 본다.
> 진행 상황과 다음 작업은 [HANDOFF.md](./HANDOFF.md) 참고.

---

## 1. 개요

하나의 웹앱, 역할별 3개 화면.

| 모드 | 사용자 | 기기 | 핵심 기능 |
|------|--------|------|-----------|
| **스크린** (viewer) | 참가자 전원 + 무대 프로젝터 | 대형 스크린, 개인 폰 | 브래킷 실시간 표시, LIVE 표시, 결과 공개 연출, 우승 화면 |
| **심사** (judge) | 심사위원 또는 간사 | 태블릿 / 폰 / 노트북 | 승자 선택 + 코멘트 제출 (**점수 입력 없음**) |
| **운영** (admin) | 운영팀 | 노트북 | 팀 관리, 경기 시작/종료, 집계 열람, 결과 공개, 대진 추첨, 초기화 |

**접근 제어** — 심사 화면은 심사 코드, 운영 화면은 운영 PIN으로 입장. 스크린은 무제한 공개.
코드·PIN은 운영 화면에서 변경 가능.

---

## 2. 토너먼트 규칙 (행사 규정 기반 — 변경 불가)

- 본선 **8팀** = 트랙별 2팀 × 4트랙 (`SJF` / `AAC` / `LIKELION` / `OPEN`).
- **라운드 1**: 1:1 × 4경기, **같은 트랙 2팀끼리** 대결. 발표 5분 + 공통 Q&A 8분.
- **라운드 2**: 1:1 × 2경기, **트랙 간 랜덤 매칭**. 라이브 시연 3분 + 기술 Q&A 5분.
  - R1 승자는 트랙별 1팀씩이므로, 4팀을 무작위로 짝지으면 자동으로 트랙 간 매칭이 된다. **별도 제약 불필요.**
- **결선**: 단판. 라스트 어필 100초 + 심사위원 합의 100초.
- 각 경기 종료 후 진출팀 즉시 발표 → 운영자의 "결과 공개" 액션이 곧 발표다.
- 라운드 2에서 **시연 장애** 발생 시 제출한 시연 영상으로 대체 가능하되 실현 가능성(20점) 항목에 반영
  → 심사 화면에 팀별 "영상 대체 진행" 체크박스를 두고 기록으로 남긴다.

---

## 3. 심사 방식 (확정된 결정 사항)

- **승자 선택 + 코멘트만.** 기준별 점수 입력 없음. 모든 라운드 동일.
- **심사위원 명단제** (2026-08-17 결정): 운영 화면에 심사위원 이름을 미리 등록하고,
  **명단에 없는 명의의 제출은 서버가 거부**한다. 심사 코드가 새어도 가짜 표가 집계에 못 섞인다.
  - 심사 화면 입장: 심사 코드 검증 → **명단에서 본인 이름 선택** (직접 타이핑 없음 → 오타·표기 흔들림 차단)
  - 명단은 공개 상태에 포함하지 않는다. 코드 검증을 통과한 요청에만 내려준다
  - 동명이인은 명의가 충돌하므로 등록 시 구분자를 붙인다 (예: "김OO A")
- 입력 주체는 양쪽 지원:
  1. 심사위원이 각자 기기로 직접 제출
  2. 간사 1명이 **대리 입력 모드**를 켜고 심사위원 명의를 **명단에서 지정**해 대신 제출
- 제출은 **결과 공개 전까지 재제출 = 덮어쓰기** (같은 명의 기준).
  - 명의 키(`judgeSlug`)는 이름을 NFC 정규화 + 공백 제거 + 소문자화한 값
  - **live 가 아닌 경기의 제출은 서버가 거부** (공개 직후 지연 도착한 제출이 기록을 바꾸는 것 방지)
  - 제출 시각(ts)은 서버가 찍는다. 이름 30자·코멘트 500자 서버 측 제한
- 심사위원 상호 간 표는 **공개 전까지 비공개**. 집계(득표수·코멘트·영상 대체 플래그)는
  운영 화면에서만 열람. 결과 공개 후에는 스크린 정지 화면이 표 카드에 **심사위원 이름을
  달아** 명의-표 연결을 무대에 공개한다 (2026-08-22 저녁 운영자 결정 — 8/18 "명의를
  떼고 공개" 번복, §6.1 명의 표기 참조).
- **동표 시 시스템이 자동 결정하지 않는다.** 운영자 화면에 동표 경고만 표시하고,
  사전 합의된 규칙(심사위원장 캐스팅보트 등)으로 운영자가 수동 결정.

---

## 4. 데이터 모델

### 4.1 전역 상태 (단일 문서, 운영자만 쓰기)

```jsonc
{
  "teams": [ // 정확히 8개, index 가 팀 ID
    { "emoji": "🦁", "school": "OO대", "team": "팀명", "track": "SJF" }
    // track ∈ SJF | AAC | LIKELION | OPEN
    // 실제 에셋 적용 시 emoji 대신 캐릭터 이미지 키로 교체 가능
  ],
  "matches": [
    // votes: 결과 공개 시점의 표 (§6.1 투표 오픈 연출). 공개 전 null,
    //        공개 시 서버가 순서를 셔플(결선은 연출 정렬)해 저장
    // voteNames: votes 와 같은 순서의 심사위원 이름 (§6.1 2026-08-22 저녁 —
    //        정지 화면 명의 표기. §3 "명의를 떼고 공개"는 이날 번복)
    { "id": "R1-1", "round": 1, "a": 0,    "b": 1,    "status": "ready", "winner": null, "votes": null },
    { "id": "R1-2", "round": 1, "a": 2,    "b": 3,    "status": "ready", "winner": null },
    { "id": "R1-3", "round": 1, "a": 4,    "b": 5,    "status": "ready", "winner": null },
    { "id": "R1-4", "round": 1, "a": 6,    "b": 7,    "status": "ready", "winner": null },
    { "id": "R2-1", "round": 2, "a": null, "b": null, "status": "ready", "winner": null }, // 추첨 전 null
    { "id": "R2-2", "round": 2, "a": null, "b": null, "status": "ready", "winner": null },
    { "id": "F",    "round": 3, "a": null, "b": null, "status": "ready", "winner": null }
  ],
  "judges": [], // 심사위원 명단 (§3). 운영 화면에서 등록, 명단 밖 명의 제출 거부
  "judgeCode": "ANIMAL",
  "adminPin": "0825",
  "rev": 1 // 낙관적 버전. 수신 시 rev 가 낮은 스냅샷은 무시
}
```

- 초기 `judgeCode`/`adminPin` 은 공개 저장소에 있는 값이므로 **행사 전 운영 화면에서 반드시 변경**한다
  (리허설 체크리스트 필수 항목, PIN 은 6자리 이상 권장). 시스템이 강제하지는 않는다.

- `status`: `ready`(대기) → `live`(진행 중, 심사 오픈) → `done`(결과 공개됨)
- **`live` 는 동시에 1경기만** — 경기 시작 시 기존 live 는 ready 로 되돌린다.
- **팀 배치 규칙**: `teams[0]–[1]`, `[2]–[3]`, `[4]–[5]`, `[6]–[7]` 이 R1 대진.
  같은 트랙끼리 인접 배치하도록 운영 화면에 안내 문구 표시.
  (검증 로직 추가 시: 인접 쌍 트랙 불일치 경고)

### 4.2 심사 제출 (심사위원별 개별 레코드)

키: `vote:{matchId}:{judgeSlug}` — **명의별 키 분리로 동시 제출 충돌 방지**

```jsonc
{
  "name": "심사위원 이름",
  "winner": "A" | "B",
  "comment": "선택 입력",
  "videoA": false, // 라운드 2 전용: A팀 영상 대체 여부
  "videoB": false,
  "ts": 1756000000000
}
```

---

## 5. 상태 전이 / 운영 플로우

```
[팀 8개 입력] → R1-1~4 순차 진행:
  경기 시작(live) → 심사 제출 수집 → 집계 확인 → 승자 지정 + 결과 공개(done)
→ 4경기 완료 시 [라운드 2 랜덤 대진 추첨] 활성화 (Fisher–Yates 셔플로 R2 슬롯 채움)
→ R2 2경기 동일 진행
→ 2경기 완료 시 [결선 대진 확정] 활성화
→ 결선 진행 → 결과 공개 시 스크린이 우승 화면으로 전환
```

- **결과 취소 없음** (2026-08-16 결정): `done` 은 종착 상태다. 결과 공개가 곧 현장 발표라
  롤백해도 발표를 되돌릴 수 없고, 하위 대진 무효화 정책 문제도 함께 사라진다.
  대신 "결과 공개" 버튼에 **확인 다이얼로그를 필수**로 두어 오조작을 사전에 막는다.
  잘못 공개된 경우의 최후 수단은 전체 초기화.
- **백업 모드**: 심사 제출이 0건이어도 운영자가 직접 승자를 지정해 공개할 수 있어야 한다.
  현장 네트워크 장애 시 **운영자 입력만으로 브래킷이 굴러가는 것이 최소 보장선.**
- **전체 초기화**: 상태 + 모든 vote 레코드 삭제. 리허설 후 본 행사 전 사용.
  경기 ID(`R1-1`…)가 재사용되므로 **vote 를 먼저 지우고 상태를 리셋**한다 —
  순서가 반대면 삭제 실패 시 리허설 표가 본 행사 집계에 섞인다.
  심사 코드·운영 PIN·팀 명단·심사위원 명단은 유지 (팀 명단은 옵션으로 비울 수 있게).

---

## 6. 화면별 명세

### 6.1 스크린 (viewer)

- 상단: 대회 워드마크 + "본선 토너먼트" (기존 브랜드 에셋 적용 영역)
- **라이브 배너**: 현재 live 경기 정보. live 없으면 대기 문구, 결선 done 이면 우승 테이크오버(아래)
- **브래킷**: **피라미드형** (2026-08-20 결정, 8/18 수렴형 폐기) — 결승(상) ←
  준결승 2(중) ← R1 8팀 개별 카드(하). 수렴형은 좌우 사이드의 R1 이 같은 사이드
  준결승으로 고정 진출하는 것처럼 읽혔는데 R2 는 랜덤 추첨이라 오해를 만든다.
  하단(R1)↔중단(R2)은 선으로 잇지 않고, R1 종료 후 승자 슬롯에 "→ R2-N 진출"
  배지로 표기. "랜덤 추첨" 안내 밴드는 넣지 않는다 (8/20 피드백: 시각 소음).
  준결승 카드는 자기 아래 두 열의 **정중앙**에 정렬 (2026-08-22 운영자 결정 —
  8/20 '정렬 안 함'을 번복. 쏠려 보인다는 피드백. 연결선 금지는 유지 — 절반 셀
  그리드 구조라 연결선 끝점 = 준결승 중심 = 두 열 중점이 수치 없이 자동 성립).
  **세 라운드 전부 R1 과 같은 개별 카드 열** (2026-08-22 오후 개편): 슬롯 +
  세로 스텁 + 카드 2장 + VS. 슬롯 라벨은 R1 = **트랙명(트랙 컬러)** — 같은 트랙
  대결이라 두 팀의 트랙이 곧 경기 이름이다, R2 = "FINAL 진출", 결선 = "CHAMPION".
  카드의 트랙 배지는 제거하고 그 폭을 학교명이 쓴다 (한 줄 + 말줄임).
  **헤더(로고·행사 표기)·푸터·안내 문구 전부 제거** (2026-08-22 오후 — 8/22 오전
  '좌상단 고정'을 번복. 무대는 연출과 대진표만): 우승 무대의 좌상단 블록 포함.
  결승↔준결승 연결선은 고정 구조라 항상 표시 (확정 전 점선). 모바일은 세로 스택
- **경기 카드**: 팀 2줄(캐릭터·팀명·대학·트랙 배지) + VS. 상태별 표현:
  - `live`: 테두리 강조 + LIVE 배지(점멸)
  - `done`: 승자 하이라이트 + "승" 마크, 패자 dim 처리
  - 대진 미확정 슬롯: "진출팀 대기" 플레이스홀더
- **대결 포커스** (2026-08-18 결정): live 경기가 생기면 대진표 대신 해당 대결을
  크게 — 두 팀 대형 카드 + VS + **경기 표기 "ROUND 1-2"**(디스플레이 서체, 오렌지.
  결선은 FINAL). live 가 없으면 대진표로 복귀
  - 중앙에 LIVE 칩을 두지 않는다 (2026-08-19 결정): 헤더 라이브 박스와 중복이고,
    포커스 화면 자체가 진행 중 경기를 뜻한다. 라운드 정보가 그 자리를 가져간다
  - 표기는 운영 코드(R1-2)를 풀어 쓴 형태 (2026-08-19 결정): 라운드만으로는 같은
    라운드 네 경기가 구분되지 않고, 코드(R1-2)만으로는 관객이 못 읽는다.
    운영 코드 체계와 1:1 이라 스태프 호출("R1-2 준비")과도 이어진다
  - **배경 연출** (2026-08-19 추가): 대각 스플릿(좌 A 오렌지 / 우 B 블루 진영 톤,
    표 칩과 같은 SIDE_COLORS) + 카드 뒤 스포트라이트 + 중앙 빔. 등장 시 페이드인·빔
    스윕 1회, 이후 글로우 4초 숨쉬기(우승 연출과 동일 리듬). `prefers-reduced-motion` 존중
- **결과 공개 — 2단계** (2026-08-22 전면 개편, 8/18 자동 시퀀스를 대체):
  1. 운영자 [공개] → 스크린은 **표 오픈 정지 화면** — 뒷면 칩이 1.6초
     간격으로 앞면(표받은 팀 캐릭터 카드)으로 뒤집히고, 전부 열린 뒤 **머문다**.
     이 정지 구간이 심사위원 코멘트 시간이다 (길이 무제한, 새로고침에도 유지 —
     `done && !announced` 상태 파생).
     화면 구성 (2026-08-22 심야 확정 — "두 개만 남겨서 화면 꽉꽉", 같은 날 저녁의
     멘트+히어로 구성을 원거리 가독을 위해 재번복): **라운드 표기 + 열린 칩만 세는
     실시간 집계**(최대 크기)와 **표 카드**(2xl 카드 폭 336px) 둘뿐. 큰 행사장
     뒷줄까지 읽히는 게 목표. **카드 밑에 심사위원 이름**(text-3xl) — 플립 완료와
     함께 등장 (뒷면 상태에서 이름이 먼저 보이면 다음 표를 예고하는 꼴이라 플립 뒤에만).
     카드는 **클리핑 래퍼 없이 에셋 원형** (2026-08-22 심야 — 베이크 곡률이 앞면
     5.63~5.78%·뒷면 6.06% 로 상이해 CSS 곡률로는 못 맞춤, 실측 후 래퍼 제거).
     진영색은 카드 알파 모양을 따라가는 drop-shadow 글로우 + 이름색이 담당.
     추첨 카드도 같은 구조 (오렌지 글로우)
  2. 코멘트 후 운영자 [발표] (`announceResult`, `Match.announced`) → **결과
     화면**("결과를 발표합니다" 멘트 + 라운드 표기 + 최종 집계 + 승자 강조/진출
     확정, 6초) → 대진표 복귀 (승자 슬롯 라이즈).
  - **결선 특례** (2026-08-22 저녁, MC 큐시트 대조로 확정): 결선은 2단계가 아니라
    **[공개] 한 번**이다 — `revealResult('F')` 가 `announced` 를 즉시 세팅해 [발표]
    단계가 없다. 스크린은 공개 감지 순간 표 연출(정지 화면과 같은 구성, 패승패승승
    정렬)을 1회 재생하고 **연출이 끝나면 우승 테이크오버로 자동 전환** — 마지막
    카드가 승부를 확정한 직후 슬램이 떨어진다 (표 5장 기준 클릭 후 ~12초, 표 0건은
    즉시). 이유: 큐시트의 "5-4-3-2-1 → LED 결과 화면" 은 코멘트 정지 구간이 없고,
    정지 화면을 끼우면 카운트다운 전에 승부가 스포일된다. 우승 확정 상태로 새로고침한
    화면은 연출 없이 우승 무대 직행 (첫 폴링 가드)
  - **명의 표기** (2026-08-22 저녁, §3 번복): 정지 화면 표 카드에 심사위원 이름을
    단다 — `Match.voteNames` (votes 와 같은 순서, 서버가 {진영, 명의} 쌍으로 셔플/
    연출 정렬). "명의를 떼고 공개" 원칙(§3, 8/18)은 이 지점에서 폐기 — 어느 심사위원이
    어느 팀에 투표했는지 무대에 공개된다. 제출 **순서**는 여전히 지운다.
    도입 전 데이터(voteNames 없음)는 이름 없이 카드만 그린다
  - 표 0건(백업 모드)은 정지 화면이 없어 공개 즉시 발표로 간주 (`isAnnounced`)
  - **결선 표 공개 순서** (2026-08-22): 셔플 대신 연출 정렬 — 패자 표가 남는 동안
    [패자, 승자] 번갈아, 남은 승자 표는 뒤에 (3:2 → 패승패승승, 마지막 장이 확정.
    4:1/5:0 → 표 적은 쪽 먼저). R1·R2 는 기존 셔플 유지. {진영, 명의} 쌍을 통째로
    재배열한다 (2026-08-22 저녁 명의 표기 도입 — `finalRevealOrder` 제네릭화)
- **대진표 시각 개편 2차** (2026-08-22 심야): ① **추첨 전 준결승은 중앙 집결 박스
  하나** — R1 승자가 나올 때마다 카드가 박스 안에 모이고(빈 슬롯은 "R1-N 승자"),
  추첨 후에 두 쌍(FINAL 진출) 배치로 전환. 연결선도 추첨 전엔 중앙 스텁 하나만.
  빈 슬롯·대기 카드는 물음표 카드 에셋(card-back-Q)로 그린다 (2026-08-23 —
  회색 "?" 박스 대체, CharacterArt 폴백 포함). **결승 대기 카드는 R2 승자가
  나오는 즉시 표시로 선반영** (2026-08-23 — 좌=R2-1·우=R2-2 승자, setFinal
  a/b 매핑과 동일. 확정·경기 시작 가드는 여전히 [결선 확정] 몫).
  결선 열의 '진출팀 대기' 쌍은 **항상 보인다** (2026-08-23 재복원 — 추첨 전
  숨김을 하루 시도했으나 집결 4팀이 곧장 챔피언으로 가는 것처럼 읽혀 철회.
  결승이라는 단계가 화면에 보여야 한다).
  ② R1 승자 슬롯: 트랙 **글자색 → 배경색** (흰 글자 — 네 트랙색 모두에서 안전),
  표기는 "SJF TRACK" 식으로 확장. done 후에는 **승자 팀명만** (2026-08-23 최종 — "트랙 유지"·"완료 시 슬롯 제거"를 같은 날 시도한 끝의 확정: 트랙명은 경기 전 예고, 종료 후 그 자리의 정보는 승자. "→ R2-N 진출" 배지는 뺌). ③ CHAMPION 슬롯 전용 디자인 (골드 그라데이션
  텍스트 + 글로우 박스 + 🏆). ④ 대진표 배경 추가 — 중앙 상단 오렌지 글로우 +
  스포트라이트 빔 2개 + 비네트 (fixed z-0, FocusLive 배경과 같은 구조)
- **R2 수동 대진 옵션** (2026-08-22 운영자 결정): `drawRound2` 에 `pairs` 를 주면
  셔플 대신 지정한 짝을 쓴다 (운영 콘솔의 '수동 대진' 패널 — R2-1 두 팀을 고르면
  나머지가 R2-2). **규정(§2) 기본은 랜덤**이며 수동은 운영 재량으로 기록한다.
  스크린 연출은 어느 쪽이든 동일한 셔플 애니메이션 — 무작위성의 출처만 다르다.
- **R2 추첨 연출** (2026-08-20 신설, 2026-08-22 저녁 3단계로 개편 — "진출 4팀을
  한군데 모은 다음 랜덤으로 돌려서 배치"): 준결승 슬롯이 채워지는 순간을 감지해
  (직전 스냅샷과 diff) 1회성 시퀀스 재생 (11초) —
  ① **모으기**: 진출 4팀의 앞면 카드가 화면 중앙 한 덩이로 모인다 (모으기 전 팀명 라벨 없음.
  공개 플립 뒤에는 **카드 위에 팀명** — 2026-08-23 재조정: 같은 캐릭터·학교가
  붙을 수 있어 팀명 필요. R2-N·VS 라벨은 원거리 가독 크기)
  ② **셔플**: 모인 채로 전부 뒷면(`card-back-0624`)으로 뒤집혀 섞인다
  ③ **배치**: 뒷면인 채 두 쌍의 자리로 흩어진 뒤 한 장씩 뒤집혀 대진 공개 → 대진표 복귀.
  중앙까지의 거리는 반응형이라 카드별 CSS 변수(--gx/--gy)로 마운트 때 실측한다.
  화면의 섞임은 연출일 뿐, 무작위성은 서버 추첨(`drawRound2`)이 만든다.
  추첨 완료 상태로 새로고침한 화면은 재생하지 않는다 (첫 폴링 가드).
  **`prefers-reduced-motion` 존중** (완성 상태 정지 표시, 도입 팀명 숨김)
- **우승 테이크오버** (2026-08-21 개편 — 디자이너 무대 연출 이식): 결선 done 이면
  전면 전환 — 스캔 라인 → 플래시 → 우승팀 카드 슬램 → CHAMPION 레터 드롭 →
  팀명 와이프 → 소속 정보. 배경은 스포트라이트 빔 2개 + 회전 광선/아우라 링 +
  컨페티 캐논 3발 + 필름 그레인·비네트. 원본은 `docs/championstage2.0.html`
  (1920×1080 고정 무대 좌표계) — 이 화면은 폰도 대상이라 이펙트 치수를 카드 폭
  배수로 환산한 반응형으로 이식했다. 카드 안쪽은 등록된 완성 카드 PNG 를 그대로
  사용 (디자이너 인계 메모 §6). **`prefers-reduced-motion` 존중**
  (흔들림·플래시·충격파·포일·컨페티는 끄고 페이드 계열만 유지, 원본 축소 규칙 동일).
  좌상단 브랜드 블록(워드마크 + 행사 표기)은 우승 무대에서도 다른 화면과 동일하게
  고정 (2026-08-22 운영자 결정 — 원본의 rail-top 텍스트 한 줄을 대체)
- **효과음** (2026-08-22 신설, 같은 날 5종으로 확장): 연출 순간별 큐 —
  | 순간 | 소리 | 규칙 |
  |---|---|---|
  | 대결 포커스(VS) 등장 | **칼 스윙**(Pixabay, 2.2초 — 2026-08-23 운영자 선곡, 오케스트라 히트 대체) + 서브 드롭 | **reduced-motion 에도 냄** — 화면 전환은 정보 |
  | 공개 시퀀스 시작 (표>0) | 덱 부채꼴 펼치기 (낮게 0.35) | reduced-motion 생략 |
  | 표 칩 / 추첨 카드 플립 시작 | 카드 놓기 4종 라운드로빈 | 플립 animationstart 동기 — 화면과 어긋날 수 없음 |
  | R2 추첨 셔플~배치 (2.9~6.3초) | **카드 믹싱**(Pixabay, 4.6초 원샷 — 2026-08-23 운영자 선곡, 셔플 루프 대체. 3.4초 재생 + 끝 0.3초 페이드) | reduced-motion 생략 |
  | 승자 발표 순간 | 칩 클래터 + **상승 플링 2음**(E5→B5 합성, 8/22 보강) | **reduced-motion 에도 냄** — 발표는 모션이 아니라 정보 |
  | 우승 카드 슬램 (0.56초) | 합성 임팩트 붐 (서브 사인 드롭 + 노이즈) | reduced-motion 생략 (슬램이 페이드로 대체되므로) |
  | 우승 CHAMPION 드롭 (1.04초) | **팡파레** 6.2초 (Hyper Ultra Fanfare, CC0) — 큐시트의 '팡파레 BGM' 을 화면이 담당 (8/22 확인: 음향 콘솔 목록에 없음) | reduced-motion 에도 냄 — 발표는 정보 |
  샘플은 Kenney Casino Audio(CC0) + Pixabay 2종(칼 스윙·카드 믹싱, Pixabay
  Content License — 출처는 public/sfx/LICENSE.txt) — `public/sfx/`, 행사장
  네트워크 대비 셀프호스트. 브라우저 자동재생 정책상 원래 첫 제스처(클릭/키) 후에만 소리가
  나는데, **무대는 `scripts/stage-launch.bat` 로 연다** (8/22 결정 — 자동재생
  허용 플래그 + 키오스크 전체화면 크롬. 클릭·F 없이 즉시 소리, 종료 Alt+F4).
  일반 브라우저로 열었을 때의 제스처 언락(클릭 1번)은 폴백으로 유지.
  재생 실패는 조용히 무시 (소리는 보조 연출, 화면 동작을 막지 않는다).
  스크린 주소는 참가자에게 **비공개 — 프로젝터 전용 운영** (2026-08-22 결정):
  소리는 무대 스피커로만 나가므로 기기별 게이팅이 불필요하다.
  BGM(음악)은 미도입 — 음원 선정·저작권 확인이 필요해 별도 결정으로 미룸.
  대결 포커스 **진행 중**(등장 임팩트 이후)·대기 브래킷에는 소리를 넣지 않는다 —
  발표·Q&A 와 행사장 PA 를 침범하지 않기 위한 의도적 무음.
  BGM(등단·드럼롤·축하·팡파레)은 **행사장 음향 콘솔 담당** (8/22 큐시트 확인) —
  화면 효과음은 그 위에 얹히는 보조 연출로 공존한다
- **무대 확대** (2026-08-22): 프로젝터 폭(≥1880px)에서 피라미드 브래킷을 `zoom` 1.18배
  — zoom 은 레이아웃이 함께 커져 연결선↔준결승 정합이 유지된다 (개별 값 확대는
  PyramidConnector 폭 재계산 필요). 대결 포커스·공개·추첨·헤더도 2xl 티어 확대
  (히어로 카드 288px, 팀명 60px 등). 근거: 1920×1080 실측에서 상하 ~300px 여백,
  R1 텍스트가 홀 거리에서 판독 불가 수준.
  **2026-08-22 저녁 2차 확대** ("화면 넓게 써라"): 세로는 zoom 1.11 로 상한이라
  **가로 방향**으로 — 브래킷 카드 폭 152→176px·슬롯 192→224px·열 간격 확대
  (R1 줄이 1920 을 좌우 여백 ~40px 까지 사용), 대결 포커스 히어로 288→320px·
  팀명 72px, 추첨 카드 176→192px, 우승 무대 --cardw 384→432px.
  전부 1080 세로 실측 통과 (대진표·포커스·결과 1080/1080, 우승 소속 줄 1015px)
- **입력 요소 없음.** 폴링으로만 갱신
- 하단 고지: "각 경기 종료 후 즉시 발표" (부스 투표 안내는 2026-08-20 삭제 —
  부스 투표를 진행하지 않는 것으로 확정)

### 6.2 심사 (judge)

- **입장**: 심사 코드 입력 (대소문자 무시 비교) → 통과 시 **명단에서 본인 이름 선택** (§3)
- **기기 폭 대응** (2026-08-18 확인): 심사위원 기기에 노트북 포함 — 768px 이상에서
  두 팀 카드를 좌우 대형 배치로 전환한다. 폰은 세로 스택 유지
- live 경기 없음 → 대기 화면 + 다음 예정 경기 안내
- live 경기 있음 → 심사 카드:
  - 두 팀 **대형 탭 카드** (캐릭터·팀명·대학·트랙) 중 택1
  - **라운드 2일 때만** 팀별 "시연 장애 → 영상 대체 진행 (실현 가능성 반영)" 체크박스 노출
  - 코멘트 textarea (선택)
  - **간사 대리 입력 모드** 토글 → 켜면 제출 명의 입력 필드 노출, 제출 버튼에 명의 표기
  - 제출 후에도 결과 공개 전까지 수정 재제출 가능 안내
- **라운드별 타이머** (2026-08-19 개정 — 클라이언트 로컬 → **운영 구동 서버 동기**.
  심사위원이 각자 누르는 로컬 타이머는 기기마다 시간이 달라 사실상 장식이었다):
  - 경기 시작 시 해당 라운드 **첫 단계가 자동 시작**, 심사 화면은 **표시 전용**
    (단계 라벨 + 남은 시간, 30초 이하 경고색, 0초 점멸). 단계 전환·재시작은 운영 화면(§6.3)
  - **시계 편차 보정** (2026-08-22): startedAt 은 서버 시각이라 기기 시계가 틀어져
    있으면 표시가 그만큼 어긋난다 — 폴링 응답의 서버 now(+CDN Age 보정)로 편차를
    기록해 `serverNow()` 로 계산한다 (lib/clock.ts). 같은 날 발견·수정한 버그:
    **콘솔 폴링 스냅샷(/api/admin/state)에 timer 가 빠져** 콘솔 타이머가 4초마다
    `--:--` 로 리셋됐다 (액션 응답에만 실려 있었음) — "운영콘솔이랑 안 맞음" 의 원인
  - R1: 발표 `5:00` / 공통 Q&A `8:00`
  - R2: 시연 `3:00` / 기술 Q&A `5:00`
  - 결선: 라스트 어필 `1:40` / 심사 합의 `1:40`
  - 서버는 **시작 시각만** 기록하고 남은 시간은 각 기기가 계산 — 폴링 지연(3초)과
    무관하게 남은 시간이 일치한다. **일시정지는 없다** (중앙 시계의 정지 상태 관리는
    당일 운영 복잡도만 높인다 — 재시작으로 갈음). 결과 공개 시 타이머 자동 해제
- 경기가 done 으로 바뀌면 입력 폼 자동 초기화 후 대기 화면 복귀

### 6.3 운영 (admin)

- **입장**: PIN
- 탭 3개:
  - **경기 진행**: 경기별 카드 목록
    - `ready` → "경기 시작"
    - `live` → **타이머 제어**(현재 단계·남은 시간 + 단계 시작/재시작 버튼, §6.2) +
      실시간 집계(4초 폴링 + 수동 새로고침: 팀별 득표수, 다득표 측 자동 강조,
      명의별 제출 내역·코멘트·영상 대체 플래그, **동표 경고**) + 팀별 "결과 공개" 버튼
    - `done` → 결과 표시 + "결과 취소"
    - 하단에 R2 추첨 · 결선 확정 버튼 (조건 미충족 시 disabled)
  - **팀 관리**: 8팀 인라인 편집 (캐릭터, 대학, 팀명, 트랙 select)
    - 캐릭터는 **운영 화면에서 `char_01~80` 그리드를 띄워 직접 선택** (2026-08-17 결정 —
      코드에 학교→캐릭터 매핑 테이블을 두지 않는다. 진출팀 확정 전에 매핑을 박을 수 없고 당일 변경도 자유로워야 함)
  - **설정**: 심사 코드, 운영 PIN, **심사위원 명단 관리(추가·삭제)**, 전체 초기화(확인 다이얼로그 권장), 데이터 공개 범위 고지

---

## 7. 동기화 / 백엔드 요구사항

> 원본 프로토타입은 Claude 아티팩트 전용 `window.storage`(공유 KV)를 사용했다.
> 자체 스택으로 옮기며 이 부분을 교체한다. **채택: Supabase (관리형 Postgres) + Next.js API 라우트.**

- 공유 KV 또는 그에 준하는 저장소
- **전역 상태**: 단일 문서, 운영자 단독 쓰기, last-write-wins + **rev 버전 가드**
- **vote**: `{matchId}:{judgeSlug}` 단위 개별 레코드 upsert, prefix 조회
- **갱신 전파**: 폴링. 스크린은 **1.5초** (2026-08-22 저녁, 3초에서 단축 — 프로젝터
  전용 전환으로 클라이언트가 소수가 되어 콘솔→스크린 반영 지연을 ~2초로 줄임),
  심사·운영 화면은 기존 주기 유지
- **폴링 부하**: `GET /api/state` 에 CDN 캐시 헤더 (`s-maxage=1,
  stale-while-revalidate=4`, 2026-08-22 저녁 2→1 단축 — 위와 같은 이유).
  클라이언트는 rev 가 낮은 스냅샷을 무시 (§4.1)
- 인증은 코드/PIN 수준의 **경량 게이트로 충분** (행사 1일용).
  단, **쓰기 API 는 최소한 서버 측에서 PIN 검증** — 클라이언트 검증만으로는 참가자가 API 직접 호출로 조작 가능
- **현장 네트워크 혼잡 대비**: 저장 실패 시 명시적 에러 표시 + 재시도, 스크린은 **마지막 정상 스냅샷 유지**
- **웹폰트 셀프호스트**: CDN(@import) 의존 제거 — 행사장 네트워크가 불안해도 타이포가 유지돼야 함
- **배포: Vercel** (2026-08-17 결정). GitHub 연동으로 main 머지 시 자동 배포, env 는 대시보드에서 설정.
  오프라인 폴백(운영 노트북 로컬 실행) 시 운영 화면은 노트북 본체에서 연다
  (LAN IP + http 로 붙으면 secure 쿠키가 안 박힘 — env 로 완화 가능)

---

## 8. 엣지 케이스 체크리스트

> **검증 기록 (2026-08-18)**: 아래 전 항목을 실서버 시나리오 16건으로 실주행, 전부 통과.
> 동표 경고·심사 폼 초기화는 실제 화면(Playwright UI)으로, 나머지는 API 가드로 확인.
> 동표 경고 증거: `docs/screenshots/admin-tie-warning-desktop.png` (2:2).
> 마지막 항목(행사 전 코드·PIN 변경)만 사람 몫으로 남음 — 리허설 체크리스트 참조.

- [x] 동표 → 자동 결정 금지, 운영자 수동 결정 (경고 문구 표시)
- [x] 결과 공개는 되돌릴 수 없음 → 공개 버튼에 확인 다이얼로그 필수 (§5, 결과 취소 없음 결정)
- [x] 심사 제출 0건 상태에서 결과 공개 → **허용** (백업 모드)
- [x] 같은 명의 중복 제출 → 덮어쓰기 (마지막 제출 유효)
- [x] 명단에 없는 명의 제출 → 거부 (§3 명단제)
- [x] live 아닌 경기에 제출 → 거부 (공개 직후 지연 제출 방지)
- [ ] 행사 전 심사 코드·운영 PIN 변경 (공개 저장소의 초기값 그대로 쓰지 않기) — **사람 몫, 리허설 때**
- [x] live 경기 전환 시 심사 화면 폼 초기화
- [x] R2 추첨은 **R1 4경기 done + R2 미추첨**일 때만, 결선 확정은 **R2 2경기 done + 결선 미확정**일 때만
- [x] 대진 미확정 경기는 시작 불가
- [x] 리허설 데이터 → 본 행사 전 전체 초기화 (초기화 동작은 검증됨 — **실행 자체는 리허설 후 사람 몫**)

---

## 9. 디자인 에셋 적용 포인트

기존 에셋으로 교체할 영역:

- 워드마크 / 로고 — **적용 완료** (2026-08-19, 공식 SVG 인라인)
- 브랜드 컬러 토큰 — **적용 완료** (2026-08-19 브랜드 가이드 수령):
  오렌지 `#ec6c01`, 진영 B 블루 `#009be4`. 컬러는 가이드 값 그대로 수정 없이 사용.
  단 LIVE(`#ff3b30`)·DONE(`#34c759`)은 기능 상태색으로 팔레트 밖 유지 —
  가이드의 브랜드 레드(`#82181d` 계열)는 다크 배경에서 배지 시인성이 없다
- **트랙별 컬러 4종** — **확정** (공식 트랙 배너 배경색 기준, 2026-08-19 배너 시안):
  SJF `#e85f9d` 핑크 / AAC `#5ab546` 그린 / LIKELION `#574b9c` 퍼플 /
  OPEN `#009be4` 스카이. 진영 B 블루도 `#009be4`라 OPEN 도트와 색이 겹치지만
  쓰이는 맥락(투표 진영 vs 트랙 표시)이 달라 허용
- 타이포 — 영문 ANTON(제목)·INTER(서브), 국문 SUIT(제목·본문). Inter 는
  순수 라틴 서브 텍스트(경기 코드, 영문 라벨)에 `font-en` 유틸로 적용
- 팀 캐릭터 (현재 emoji → 이미지 에셋, `teams` 에 이미지 키 필드 추가) — 적용 완료
- 우승 연출, LIVE 배지 · 결과 공개 애니메이션

스크린 화면은 **프로젝터 원거리 가독성** 기준(팀명 최소 15px 상당 이상, 고대비)으로 타이포 스케일 재조정 권장.

> 사용 가능한 에셋 목록은 [../README.md](../README.md) 참고. 캐릭터 이미지 80종(`public/characters/`)이 이미 들어와 있다.

---

## 10. 근거 규정 (변경 시 반드시 확인)

- 본선 구조 · 시간 · 즉시 발표: 해커톤 가이드 "본선 토너먼트" 절
- 영상 대체 시 실현 가능성 반영, 당일 코드 수정 불가: 긴급수정창구 규정 8항
- ~~부스 투표 기한(8/28 23:59): 가이드 "부스 운영" 절~~ — 2026-08-20 부스 투표
  미진행 확정으로 무효
